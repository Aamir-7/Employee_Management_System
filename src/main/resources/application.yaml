spring:
  application:
    name: management

  datasource:
    url: jdbc:postgresql://localhost:5432/management
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
